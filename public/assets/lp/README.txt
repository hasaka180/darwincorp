Drop the ad artwork here as `websites-bg.jpg` (the F1 / Dubai skyline image).
The landing page at /lp/websites picks it up automatically; without it the
page falls back to a CSS gradient in the same colours.
