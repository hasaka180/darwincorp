seasonal-offer.jpg
  The full seasonal campaign creative (1200x2572) — also the artwork used in
  emails/seasonal-offer.html.

seasonal-offer-popup.jpg
  Derived from seasonal-offer.jpg for the home-page promo modal
  (components/PromoPopup.tsx). It is the same artwork with three bands
  cross-faded together so it fits a portrait panel: the headline block
  (y 165-550), the car and skyline (y 645-1215) and the price block
  (y 1420-1815). The logo, the subtitle, the feature-icon row, the three
  cards and the CTA are dropped — the popup renders that copy as real text.
  Regenerate it from the source with a 34px cross-fade between bands.

websites-bg.jpg
  Optional. Drop the ad artwork here to use it as the /lp/websites background;
  without it the page falls back to a CSS gradient in the same colours.
